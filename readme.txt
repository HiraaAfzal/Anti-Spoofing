To run the program, Python 3.7 is required. And all the required packages are listed in the requirements.txt file. To install the packages, run the following command in the terminal:

    pip install -r requirements.txt